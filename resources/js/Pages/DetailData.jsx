import React from "react";

const DetailData = ({ data }) => {
    return (
        <div>
            <h2>{data.name}</h2>
            {/* Tambahkan komponen lainnya sesuai dengan data yang ingin ditampilkan */}
        </div>
    );
};

export default DetailData;
